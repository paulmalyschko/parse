machine:
  python:
    version: 2.7.6

dependencies:
  override:
    - ./install-appengine.sh

test:
  pre:
    - google_appengine/dev_appserver.py --skip_sdk_update_check True ./:
        background: true
    - sleep 5